In recognize.py, replace the entire tracking logic with a simpler approach:

Remove the _face_key() function entirely
Remove the pending dict and all code related to it
Remove the stable dict and all code related to it
Replace the entire tracking system inside run_recognition() with this simple approach: just use last_results like the original code did — store the latest detection results and draw them every frame. No bucketing, no hit counting, no hold timers.
Keep the announced set — still fire on_identify only once per name per session
Keep FRAME_SKIP = 2 so recognition still runs every 2nd frame

Essentially revert the tracking logic back to the original simple last_results approach, but keep the announced set to prevent duplicate attendance logging. Do not change anything else.